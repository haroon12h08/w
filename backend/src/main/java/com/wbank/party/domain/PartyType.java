package com.wbank.party.domain;

public enum PartyType {
    /** A natural person. */
    PERSON,
    /** A legal person that is not a natural person: company, partnership, charity, the bank itself. */
    ORGANIZATION
}
